<h1><%= @show.title %> </h1>

<ul>

    <li> Genre <%= @show.genre %></li>
    <li>Performer <%= @show.performer %></li>
     <li><%= @show.image %></li>
    <li>Description <%= @show.description %></li>
        <li>
       <% @places.each do |place| %>
      <div class="place">
        <p>Venue: <%= place.placevenue %></p>
        <p>Location: <%= place.placetown %></p>
        <p>Address: <%= place.placeaddress %></p>
        <p>Description: <%= place.placeinfo %></p>
      </div>
    <% end %>
    </li>
    <br/><br/>
    <p>Would you like to add a venue / venues to this show?</p>
    <button  type="button" id="add-location-btn">add a location to the show!!!!?</button>
    <%# <%= link_to "Add New Location", new_place_path(@show) %>


 <div class="placeForm" >
  <p>"placeForm"</p>
  <%= render partial: 'shows/location_form' %>
  </div>
  <%= @show_id %>
  <script>
  document.getElementById("add-location-btn").addEventListener("click", function(){
    console.log("clicked")
  document.querySelector(".placeForm").style.visibility = "visible";
});
</script> 

<td><%= button_to "Delete Show", show_path(@show), method: :delete, data: { turbo_action: "remove" }, confirm: 'Are you sure you want to delete this Show?' %></td>

  </ul>

    <br><br>
<%= link_to "AllShows", shows_path %>


<%= link_to "Create New Show", new_show_path %>
 
